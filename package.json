{
  "name": "conference-scheduler",
  "private": true,
  "scripts": {
    "start": "meteor run --inspect",
    "test": "meteor test --inspect --once --driver-package meteortesting:mocha",
    "test-app": "TEST_WATCH=1 meteor test --inspect --full-app --driver-package meteortesting:mocha",
    "visualize": "meteor --production --extra-packages bundle-visualizer"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.55",
    "eslint": "^5.6.0",
    "meteor-node-stubs": "^0.4.1",
    "simpl-schema": "^1.4.3"
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.js",
      "server": "server/main.js"
    },
    "testModule": "tests/main.js"
  }
}
